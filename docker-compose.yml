services:
  rednotebook-web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ~/.rednotebook/data:/data
    environment:
      - REDNOTEBOOK_DATA_DIR=/data
    restart: unless-stopped
